#!/usr/bin/env bash
set -Eeuo pipefail

ROOT=$(git rev-parse --show-toplevel)
if [[ "$ROOT" == "/srv/hulk-android/repos/HULK-SA-Android" ]]; then
  echo 'BLOCKED: canonical clone is not a task worktree.' >&2
  exit 1
fi
