# Network qualification

Public tests use deterministic loopback fixtures and never production credentials. The current
instrumentation covers a successful ranged probe/full-body download. Timeout, DNS, 4xx/5xx,
malformed JSON, network switching, and metered-policy expansion remain scheduled work.

